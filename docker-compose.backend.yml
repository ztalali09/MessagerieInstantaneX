services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: messagex-backend
    environment:
      - PORT=3000
    ports:
      - '3000:3000'
    restart: unless-stopped

volumes:
  postgres_data:
