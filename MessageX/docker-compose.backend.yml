services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: messagex-backend
    restart: unless-stopped
    environment:
      - PORT=3000
volumes:
  postgres_data:
