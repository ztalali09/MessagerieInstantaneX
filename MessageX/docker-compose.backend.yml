services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: messagex-backend
    depends_on:
      - postgres
    restart: unless-stopped

volumes:
  postgres_data:
